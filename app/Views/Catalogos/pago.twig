{% extends 'Catalogos/catalogo.twig' %}

{% block title 'Pagos' %}

{% block tabla %}
    <table id="table" class="table table-bordered table-condensed table-hover">
        <thead>
        <tr>
            <th>Folio</th>
            <th>Cliente</th>
            <th>Concepto</th>
            <th>Fecha</th>
            <th>Pago</th>
            <th>Del prestamo</th>
            <th></th>
        </tr>
        </thead>
        <tbody id="table-content">
        <!-- Contenido de la tabla -->
        {% for d in pago %}
            <tr>
                <td>{{ d.Folio }}</td>
                <td>{{ d.Cliente }}</td>
                <td>{{ d.Concepto }}</td>
                <td>{{ d.Fecha|date('d/m/Y') }}</td>
                <td>{{ d.Total }}</td>
                <td>{{ d.Prestamo }}</td>
                <td class="text-right">
                <div class="btn-group btn-group-toggle" data-toggle="buttons">
                  <button type="button" class="btn btn-sm btn-primary" id="option1" data-toggle="modal" data-target="#operationModal" data-id="{{ d.IdPago }}" data-model="{{ modelo }}" data-operation="Editar">
                      <i class="fa fa-edit"></i>
                  </button>
                  <button type="button" class="btn btn-sm btn-danger" id="option2" data-toggle="modal" data-target="#operationModal" data-id="{{ d.IdPago }}" data-model="{{ modelo }}" data-operation="Eliminar">
                      <i class="fa fa-trash"></i>
                  </button>
                </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
